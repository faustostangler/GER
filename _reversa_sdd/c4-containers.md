# C4 Containers Diagram — GER

> Generated by Reversa (Architect) on 2026-04-28
> Level: Detailed

```mermaid
C4Container
    title Container Diagram for GER

    Person(doctor, "Médico / Clinico", "Visualiza painéis analíticos.")

    Container(nginx, "Nginx Border Router", "Nginx", "Roteamento e SSL Termination.")
    Container(oauth_proxy, "OAuth2-Proxy", "Go", "Intercepta requisições e valida JWT.")
    
    Container(streamlit, "Analytics UI / BFF", "Python/Streamlit", "Interface rica e agregação de KPIs.")
    Container(worker, "Background Worker", "Python/Arq", "Processamento assíncrono e scraping.")

    ContainerDb(duckdb, "DuckDB (OLAP)", "DuckDB", "Persistência analítica e queries rápidas.")
    ContainerDb(redis, "Redis", "Redis", "Cache de DataFrames (Feather) e Sessões OIDC.")
    ContainerDb(redpanda, "Redpanda", "Kafka API", "Barramento de eventos (validação CFM).")

    Rel(doctor, nginx, "Acessa via web", "HTTPS")
    Rel(nginx, oauth_proxy, "Valida sessão", "HTTP")
    Rel(oauth_proxy, streamlit, "Encaminha tráfego autorizado", "HTTP")
    
    Rel(streamlit, duckdb, "Queries analíticas", "DuckDB API")
    Rel(streamlit, redis, "Cache de KPIs", "PyArrow IPC")
    
    Rel(worker, redpanda, "Consome eventos de CRM", "Kafka Protocol")
    Rel(worker, duckdb, "Escreve dados ingeridos", "Parquet / SQL")
```

## Communication Protocols

- **PyArrow IPC (Feather)**: Cache de DataFrames no Redis (~40-60% mais rápido que pickle).
- **Kafka Protocol**: Consumo assíncrono e DLQ.
