# C4 Components Diagram — GER (Hexagonal Architecture)

> Generated by Reversa (Architect) on 2026-04-28
> Level: Detailed

```mermaid
C4Component
    title Component Diagram for Hexagonal Architecture

    Container_Boundary(domain, "Domain Core (Pure Python)") {
        Component(models, "Domain Models", "Pydantic", "Entidades e Value Objects (ClinicaPolicy, DoctorProfile).")
        Component(specs, "Specifications", "Python", "Regras de filtragem (PacienteUrgenteSpec).")
    }

    Container_Boundary(application, "Application Layer") {
        Component(analytics_uc, "AnalyticsUseCase", "Python", "Orquestra KPIs e filtros analíticos.")
        Component(scraper_uc, "ScraperUseCase", "Python", "Orquestra fluxo de captura e DLQ.")
    }

    Container_Boundary(infrastructure, "Infrastructure Adapters") {
        Component(duckdb_repo, "DuckDBAnalyticsRepository", "DuckDB", "Executa RLS e queries analíticas.")
        Component(pw_scraper, "PlaywrightGerconAdapter", "Playwright", "Robô de scraping do Gercon.")
        Component(spec_trans, "DuckDBSpecificationTranslator", "Python", "Traduz specs de domínio em SQL.")
    }

    Rel(analytics_uc, models, "Usa", "In-memory")
    Rel(analytics_uc, specs, "Aplica", "In-memory")
    
    Rel(infrastructure, application, "Implementa Ports de", "Interface")
    Rel(duckdb_repo, spec_trans, "Delega tradução", "Call")
```

## Key Components

- **DuckDBSpecificationTranslator**: Traduz predicados de domínio (ex: `PacienteUrgenteSpec`) para cláusulas SQL DuckDB via pattern matching.
- **ScraperUseCase**: Controla o Circuit Breaker e o fluxo de Poison Pills.
