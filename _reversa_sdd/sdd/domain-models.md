# SDD — Domain Models

> **Component**: `src/domain/models.py` & `src/domain/identity.py`
> **Generated by**: Reversa (Writer)
> **Level**: Detailed
> **Status**: 🟢 CONFIRMADO

## 1. Description
Este componente encapsula os modelos de domínio puros e as invariantes de negócio do sistema GER. Ele é agnóstico a frameworks e garante a integridade dos dados clínicos e de autorização.

## 2. Responsibilities (MoSCoW)

- **Must**:
  - Garantir a imutabilidade de Value Objects (`ClinicaPolicy`, `MedicalCouncilRegistration`). 🟢
  - Validar regras de negócio (idade, formato de CRM, UF). 🟢
  - Proteger o acesso via `DoctorProfile.is_authorized()`. 🟢
- **Should**:
  - Permitir sobreposição de políticas via DI (Dependency Injection). 🟢
- **Could**:
  - Suportar regras multi-tenant no futuro (Single-Tenant atual é suficiente). 🟢
- **Won't**:
  - Armazenar estado persistente diretamente. 🟢

## 3. Non-Functional Requirements

- **Security**: Zero-Trust. Apenas identidades com CRM verificado têm acesso. 🟢
- **Performance**: Validações em memória ultrarrápidas via Pydantic. 🟢

## 4. Acceptance Criteria

### Scenario 1: Validação de CRM Válido (Happy Path)
- **Given**: Um número de CRM contendo apenas dígitos e uma UF válida com 2 letras.
- **When**: O objeto `MedicalCouncilRegistration` é instanciado.
- **Then**: O objeto é criado com sucesso e permanece imutável.

### Scenario 2: Tentativa de acesso por Médico Não Verificado (Falha)
- **Given**: Um `DoctorProfile` com `crm_verified = False`.
- **When**: O método `is_authorized()` é chamado.
- **Then**: Retorna `False`.

### Scenario 3: Instanciação de Política Clínica Inválida (Faixa Etária)
- **Given**: Uma tentativa de criar `ClinicaPolicy` com `idade_min = 100` e `idade_max = 50`.
- **When**: O validador do Pydantic é executado.
- **Then**: Lança um `ValueError`.

## 5. Cenários de Borda

1. **CRM com espaços ou caracteres especiais**: O sistema limpa espaços, mas rejeita caracteres não numéricos. 🟢
2. **UF em minúsculas**: O sistema converte automaticamente para maiúsculas. 🟢
