# Entity-Relationship Diagram — GER

> Generated by Reversa (Data Master) on 2026-04-28
> Phase: Revisão (Agente Independente)

Este diagrama mapea a persistência física dividida entre o banco SQLite de Auditoria/Watermark e a base analítica DuckDB.

## SQLite Database (`gercon_raw_data.db`)

```mermaid
erDiagram
    solicitacoes_raw {
        string protocolo PK
        timestamp data_captura
        int data_alteracao
        string conteudo_json
        string origem_lista
    }

    ingestion_logs {
        int id PK
        real timestamp
        real duration_seconds
        string status
        int items_ingested
        int items_failed
        int bytes_processed
        string target_lists
        string error_message
        timestamp created_at
    }

    dead_letter_queue {
        int id PK
        datetime timestamp
        string target_list
        string payload
        string error_message
    }
```

## DuckDB Analytical View (`gercon`)

A view analítica é gerada dinamicamente a partir de arquivos Parquet.

```mermaid
erDiagram
    gercon {
        string numeroCMCE PK "Protocolo"
        string entidade_classificacaoRisco_cor
        string entidade_especialidade_descricao
        string entidade_especialidade_especialidadeMae_descricao
        string medicoSolicitante
        string entidade_cidPrincipal_descricao
        string origem_lista
        timestamp dataSolicitacao
        timestamp dataCadastro
    }
```

## Relacionamentos Lógicos (Cross-Database)

Embora não existam Foreign Keys físicas:
- `gercon.numeroCMCE` <--> `solicitacoes_raw.protocolo` (Mapeamento 1:1)
